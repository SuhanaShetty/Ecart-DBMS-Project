<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile </title>
</head>
<style>
    .grid-container{
        display:grid;
        grid-template-columns:repeat(2,1fr);
        grid-gap:10px;
    }
    img {
        width:100%;
        height:100%;
        object-fit:cover;
    }
</style>
<body>
    <h1>PROFILE</h1>
    <hr/>
    <% if(images.length> 0) { %>
        <% images.forEach((image)=>{ %>
            <div class="grid-container">
                <h2>
                    <%= profiles.pname %>
                    <%= profiles.pemailid %>
                    <%= profiles.pphone_number %>
                    <%= profiles.pexperience %>
                    <%= profiles.pach %>
                </h2>
                <img src="data:<% profiles.image.contentType %>;base64,<%= profiles.image.data.toString('base64') %>">
            </div>
        <% }); %>
            <% } else { %>
            <p>NO images uploaded yet.</p>
            <%} %>
</body>
</html>